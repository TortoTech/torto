## Conditional review
Re-read the original to resolve `local_validation_error` in the untrusted
`proposal`. A proposed rendering may be attached; if absent, compare the proposal
text directly with the original. Check the complete expression, not just the
reported error. A successful parse is not proof of a correct transcription.
Correct only what the original supports; if faithful supported notation remains
uncertain, return unreadable. Review only the IDs supplied in this request.
