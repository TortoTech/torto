## Feature

- Recognize formulas written as text and render them as mathematics, preserving their placement, copying, and translation behavior.
- Replace website addresses with clickable internet icons in unified typesetting, including common bare domains, while preserving links through translation.
- Configure the AI layout model's reasoning effort alongside its model selection.

## Improvement

- Recognize section titles written as ordinary paragraphs, with or without numbers, without a separate heading review request.
- Process AI layout in stable subsection batches of up to 5,000 characters, prioritizing visible content and reusing pending work when scrolling within a subsection.
- Limit formula image batches to five originals and simplify recognition instructions to reduce unnecessary requests.
- Ignore redundant semantic labels and identical results instead of retrying valid recognition responses.
- Close footnote popups when clicking elsewhere in the reading area.

## Fix

- Recover images and captions nested in complex ebook markup so illustrations display separately from their captions.
- Convert inherited italic styling to bold for Chinese text in unified typesetting, consistently with inline emphasis.
- Correct justified line endings and hyphen geometry when words contain ligatures.
- Keep activation borders visible around images and tables, including after sentence splitting or closing image previews, and highlight table annotations correctly.
- Preserve initials in translated Chinese names when splitting text into sentences.

[Full Changelog](https://github.com/TortoTech/torto/compare/v0.6.1...v0.6.2)

<details>
<summary>中文更新说明</summary>

## Feature

- 识别以文字编写的公式并渲染为数学公式，保留其位置、复制和翻译行为。
- 统一版式将网址替换为可点击的互联网图标，支持常见无前缀域名，并在翻译后保留链接。
- 在 AI 排版模型配置中添加思考等级选项。

## Improvement

- 识别以普通段落编写的小节标题，兼容有编号和无编号标题，不再单独请求标题复核。
- AI 排版按小节固定分批，每批最多 5,000 字符，优先处理可见内容，同小节滚动时复用在途任务。
- 公式图片每批最多处理五张原图，并精简识别说明，减少不必要的请求。
- 忽略重复语义标注和完全相同的结果，避免对有效识别响应发起重试。
- 点击阅读区域其他位置时关闭脚注弹窗。

## Fix

- 正确解析复杂嵌套结构中的图片和图注，让插图与图注分开显示。
- 统一版式下，将中文继承的斜体样式转为加粗，与行内强调处理保持一致。
- 修复包含连字的单词导致的两端对齐行尾偏差和连字符高亮几何问题。
- 保持图片和表格的激活边框可见，修复按句分段或关闭图片预览后的边框问题，并正确高亮表格注释。
- 按句分段时保留中文译文中姓名的缩写字母。

[完整更新记录](https://github.com/TortoTech/torto/compare/v0.6.1...v0.6.2)

</details>
